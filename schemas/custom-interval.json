{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec.geodb.io/exploration/v0.1.0/custom-interval.json",
  "title": "Custom interval",
  "description": "A user-defined downhole interval. THE TYPE IS DATA: the row names its own interval type, and carries either a categorical value or a numeric measure.\n\nGENERATED from the normative OpenAPI document (`spec/openapi.yaml`) — do not edit by hand; run `python scripts/regenerate.py`. The OpenAPI document is normative for the wire; this file is the same contract for one record type, readable on its own.",
  "x-generated-by": "scripts/regenerate.py from spec/openapi.yaml (geoDB Open Exploration Protocol v0.1.0)",
  "x-openapi-component": "DrillCustomIntervalRead",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer"
    },
    "project": {
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": true,
      "description": "Natural key of the project: `{\"name\", \"company\"}`."
    },
    "bhid": {
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": true,
      "description": "Natural key of the drill hole: `{\"hole_id\", \"project\", \"company\"}`."
    },
    "depth_from": {
      "type": "number",
      "format": "double",
      "description": "Top of this interval, measured ALONG the hole from the collar, in `length_units`."
    },
    "depth_to": {
      "type": "number",
      "format": "double",
      "description": "Base of this interval, measured ALONG the hole from the collar, in `length_units`."
    },
    "display_units": {
      "type": [
        "string",
        "null"
      ],
      "description": "Units the *_display strings on this record are formatted in, from the project's display preference. Presentation only: the stored values remain in `length_units`."
    },
    "xyz_from": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "number"
      },
      "description": "Desurveyed position at `depth_from`, as `[x, y, z]` on the PROJECT LOCAL GRID in metres — not WGS84, and not the record's own `epsg`. Null when the hole has no survey to desurvey against."
    },
    "xyz_to": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "number"
      },
      "description": "Desurveyed position at `depth_to`, as `[x, y, z]` on the project local grid in metres. Null when the hole has no survey."
    },
    "type": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer"
        },
        "name": {
          "type": "string",
          "description": "Human label, e.g. \"Redox\".",
          "maxLength": 150
        },
        "code": {
          "type": "string",
          "description": "Safe identifier (becomes an ODBC view name), e.g. \"redox\". Lowercase letters/digits/underscore; must start with a letter. Unique per project.",
          "pattern": "^[a-z][a-z0-9_]*$",
          "maxLength": 40
        },
        "mode": {
          "enum": [
            "categorical",
            "numeric"
          ],
          "type": "string",
          "description": "'categorical' (interval rows carry a CustomIntervalValue) or 'numeric' (interval rows carry a measure + this type's unit).\n\n* `categorical` - Categorical\n* `numeric` - Numeric"
        },
        "unit": {
          "type": "string",
          "description": "Unit for numeric types (e.g. '%', 'g/t'). Blank for categorical types.",
          "maxLength": 30
        }
      },
      "required": [
        "code",
        "id",
        "name"
      ],
      "additionalProperties": false
    },
    "value": {
      "type": "object",
      "description": "The analyte value, as a DECIMAL STRING rather than a JSON number — precision is preserved exactly as the laboratory reported it, and parsing it as a float will silently round it. Parse it with a decimal type. Its units are the sibling `units` field: units are per VALUE, not per sample and not per element, because one sample can carry values from two laboratories at different units.",
      "properties": {
        "id": {
          "type": "integer"
        },
        "name": {
          "type": "string",
          "maxLength": 150
        }
      },
      "required": [
        "id",
        "name"
      ],
      "additionalProperties": false
    },
    "measure": {
      "type": [
        "number",
        "null"
      ],
      "format": "double"
    },
    "custom_interval_set": {
      "type": [
        "string",
        "null"
      ]
    },
    "images": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true
      },
      "description": "Attached records, each as an object carrying its id, its natural key and display metadata. Soft-deleted attachments are omitted."
    },
    "notes": {
      "type": [
        "string",
        "null"
      ]
    },
    "date_collected": {
      "type": [
        "string",
        "null"
      ],
      "format": "date"
    },
    "collected_by": {
      "type": [
        "string",
        "null"
      ],
      "maxLength": 50
    },
    "length_units": {
      "enum": [
        "M",
        "FT"
      ],
      "type": "string",
      "description": "* `M` - Meters\n* `FT` - Feet"
    },
    "date_created": {
      "type": [
        "string",
        "null"
      ],
      "format": "date"
    },
    "last_edited": {
      "type": [
        "string",
        "null"
      ],
      "format": "date"
    }
  },
  "patternProperties": {
    "^cf_[a-z0-9_]+$": {
      "description": "Custom field. Any number of `cf_*` keys may be present: they are the project's user-defined fields, flattened to top level. The set of keys and their types vary BY PROJECT and BY MODEL — discover them at `/api/v2/model-schemas/?project_id=<id>`, never by assuming a fixed set. Values are whatever the customer stored (string, number, boolean or null). Calculated custom fields appear under the same prefix and are read-only."
    }
  },
  "required": [
    "bhid",
    "custom_interval_set",
    "date_created",
    "depth_from",
    "depth_to",
    "display_units",
    "id",
    "images",
    "last_edited",
    "project",
    "type",
    "value",
    "xyz_from",
    "xyz_to"
  ],
  "additionalProperties": false
}
