{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec.geodb.io/exploration/v0.1.0/drill-sample.json",
  "title": "Drill sample",
  "description": "A sample cut from an interval of core or cuttings, and sent to a laboratory.\n\nGENERATED from the normative OpenAPI document (`spec/openapi.yaml`) — do not edit by hand; run `python scripts/regenerate.py`. The OpenAPI document is normative for the wire; this file is the same contract for one record type, readable on its own.",
  "x-generated-by": "scripts/regenerate.py from spec/openapi.yaml (geoDB Open Exploration Protocol v0.1.0)",
  "x-openapi-component": "DrillSampleRead",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer"
    },
    "name": {
      "type": "string",
      "maxLength": 150
    },
    "project": {
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": true,
      "description": "Natural key of the project: `{\"name\", \"company\"}`."
    },
    "bhid": {
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": true,
      "description": "Natural key of the drill hole: `{\"hole_id\", \"project\", \"company\"}`."
    },
    "depth_from": {
      "type": "number",
      "format": "double",
      "description": "Top of this interval, measured ALONG the hole from the collar, in `length_units`."
    },
    "depth_to": {
      "type": "number",
      "format": "double",
      "description": "Base of this interval, measured ALONG the hole from the collar, in `length_units`."
    },
    "display_units": {
      "type": [
        "string",
        "null"
      ],
      "description": "Units the *_display strings on this record are formatted in, from the project's display preference. Presentation only: the stored values remain in `length_units`."
    },
    "xyz_from": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "number"
      },
      "description": "Desurveyed position at `depth_from`, as `[x, y, z]` on the PROJECT LOCAL GRID in metres — not WGS84, and not the record's own `epsg`. Null when the hole has no survey to desurvey against."
    },
    "xyz_to": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "number"
      },
      "description": "Desurveyed position at `depth_to`, as `[x, y, z]` on the project local grid in metres. Null when the hole has no survey."
    },
    "xyz_from_wgs84": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "number"
      },
      "description": "Desurveyed position at `depth_from` as `[longitude, latitude, elevation]` in WGS84 degrees. The WGS84 twin of `xyz_from`."
    },
    "xyz_to_wgs84": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "number"
      },
      "description": "Desurveyed position at `depth_to` as `[longitude, latitude, elevation]` in WGS84 degrees."
    },
    "images": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true
      },
      "description": "Attached records, each as an object carrying its id, its natural key and display metadata. Soft-deleted attachments are omitted."
    },
    "notes": {
      "type": [
        "string",
        "null"
      ]
    },
    "date_collected": {
      "type": [
        "string",
        "null"
      ],
      "format": "date"
    },
    "collected_by": {
      "type": [
        "string",
        "null"
      ],
      "maxLength": 50
    },
    "length_units": {
      "enum": [
        "M",
        "FT"
      ],
      "type": "string",
      "description": "* `M` - Meters\n* `FT` - Feet"
    },
    "lab_status": {
      "description": "Lab workflow status (Collected, Submitted, Analyzed)\n\n* `PL` - Planned\n* `CO` - Collected\n* `SU` - Submitted\n* `AN` - Analyzed",
      "oneOf": [
        {
          "enum": [
            "PL",
            "CO",
            "SU",
            "AN"
          ],
          "type": "string",
          "description": "* `PL` - Planned\n* `CO` - Collected\n* `SU` - Submitted\n* `AN` - Analyzed"
        },
        {
          "enum": [
            ""
          ]
        },
        {
          "type": "null"
        }
      ]
    },
    "assay": {
      "type": [
        "string",
        "null"
      ]
    },
    "submittal": {
      "type": [
        "integer",
        "null"
      ]
    },
    "submittal_name": {
      "type": [
        "string",
        "null"
      ]
    },
    "package_number": {
      "type": [
        "string",
        "null"
      ]
    },
    "drill_sample_set": {
      "type": [
        "object",
        "null"
      ],
      "description": "The sampling pass this interval belongs to, or null when it has not been assigned to one. A pass groups the intervals sampled together in one campaign, so two passes may cover the same depths for different purposes.",
      "properties": {
        "id": {
          "type": "integer"
        },
        "name": {
          "type": "string"
        },
        "kind": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "date_created": {
      "type": [
        "string",
        "null"
      ],
      "format": "date"
    },
    "last_edited": {
      "type": [
        "string",
        "null"
      ],
      "format": "date"
    },
    "metal_equivalent_config": {
      "type": [
        "string",
        "null"
      ],
      "description": "Name of the price deck behind the metal-equivalent column. The number never travels without its deck."
    },
    "metal_equivalent_target": {
      "type": [
        "string",
        "null"
      ],
      "description": "Target element of the metal-equivalent calculation."
    },
    "metal_equivalent_blank_reason": {
      "type": [
        "string",
        "null"
      ],
      "description": "Machine-readable reason a metal-equivalent value is blank, when it is."
    }
  },
  "patternProperties": {
    "^cf_[a-z0-9_]+$": {
      "description": "Custom field. Any number of `cf_*` keys may be present: they are the project's user-defined fields, flattened to top level. The set of keys and their types vary BY PROJECT and BY MODEL — discover them at `/api/v2/model-schemas/?project_id=<id>`, never by assuming a fixed set. Values are whatever the customer stored (string, number, boolean or null). Calculated custom fields appear under the same prefix and are read-only."
    },
    "^[A-Z][a-z]?Eq(\\s.+)?$": {
      "description": "Metal-equivalent value (e.g. `AuEq ppm`), present only when the project has a price deck. The key names the target element and its units; the accompanying `metal_equivalent_*` keys name the deck behind it."
    }
  },
  "required": [
    "assay",
    "bhid",
    "date_created",
    "depth_from",
    "depth_to",
    "display_units",
    "drill_sample_set",
    "id",
    "images",
    "last_edited",
    "name",
    "package_number",
    "project",
    "submittal",
    "submittal_name",
    "xyz_from",
    "xyz_from_wgs84",
    "xyz_to",
    "xyz_to_wgs84"
  ],
  "additionalProperties": false
}
